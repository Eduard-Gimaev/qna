.div 
  = link_to 'Back', questions_path

.question
  = render @question


.answers
  - if @answers.any?
    p = '____________________________________________________________'
    div id='answers-list' data-question-id=@question.id
    = render @answers  
    p = '____________________________________________________________'
.new-answer
  = render 'answers/form'

    
