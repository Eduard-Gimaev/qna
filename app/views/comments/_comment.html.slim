div.comment
  .comment-body
    = comment.body
    .comment-meta
      | #{comment.user.email} on #{comment.created_at.strftime('%Y-%m-%d %H:%M')}
  - if policy(comment).destroy?
    div.crud-buttons
      = button_to 'Delete', comment_path(comment), method: :delete, remote: true